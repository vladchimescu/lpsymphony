
SYMPHONY_LIBS = @SYMPHONY_LIBS@
PKG_CPPFLAGS = @SYMPHONY_INCLUDE_PATH@
PKG_LIBS = @SYMPHONY_LIB_PATH@ $(SYMPHONY_LIBS) $(LAPACK_LIBS) \
           $(BLAS_LIBS) $(FLIBS)

.PHONY: all @SYMPHONY_TS@

all: $(SHLIB)
$(OBJECTS): @SYMPHONY_TS@ 

SYMPHONY.ts:
	@(cd SYMPHONY && make && make install)
	touch $@
